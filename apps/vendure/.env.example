# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3001

# Database Configuration
DB_TYPE=better-sqlite3  # Options: better-sqlite3, postgres
DB_DATABASE_FILE=vendure-db.sqlite  # For SQLite
DATABASE_URL=vendure-db.sqlite  # Legacy support

# Vendure Auth
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=superadmin123  # Change in production
COOKIE_SECRET=your-secure-cookie-secret-here

# Asset Storage Configuration
# ASSET_STORAGE: 'local' (default) or 's3' (R2, MinIO, AWS S3)
ASSET_STORAGE=local
ASSET_UPLOAD_DIR=./static/assets  # Local directory for development

# S3/R2/MinIO Configuration (when ASSET_STORAGE=s3)
# These are REQUIRED when ASSET_STORAGE=s3:
# S3_BUCKET=tooly-assets
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key

# Optional S3 settings:
# S3_REGION=auto                    # 'auto' for R2, 'us-east-1' for AWS, etc.
# S3_ENDPOINT=                      # Custom endpoint for R2/MinIO (leave empty for AWS S3)
# S3_FORCE_PATH_STYLE=false         # Set 'true' for MinIO, 'false' for R2/AWS

# Cloudflare R2 Example:
# ASSET_STORAGE=s3
# S3_BUCKET=tooly-assets
# S3_REGION=auto
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# S3_ACCESS_KEY_ID=your-r2-access-key
# S3_SECRET_ACCESS_KEY=your-r2-secret-key
# S3_FORCE_PATH_STYLE=false

# MinIO (NAS) Example:
# ASSET_STORAGE=s3
# S3_BUCKET=tooly-assets
# S3_REGION=us-east-1
# S3_ENDPOINT=http://nas.local:9000
# S3_ACCESS_KEY_ID=minio-access-key
# S3_SECRET_ACCESS_KEY=minio-secret-key
# S3_FORCE_PATH_STYLE=true

# CDN/Asset URL Configuration
CDN_URL=  # Leave empty for development, e.g., https://cdn.example.com for production
ASSET_URL_PREFIX=http://localhost:3001/assets/  # Public URL for assets

# =============================================================================
# STRIPE PAYMENT CONFIGURATION
# =============================================================================
# Note: Stripe API keys are configured PER PAYMENT METHOD in Admin UI
# (Settings → Payment Methods → Create → Handler: "Stripe payments")
#
# For local development with Stripe CLI:
#   1. Install Stripe CLI: scoop install stripe (Windows) or brew install stripe (macOS)
#   2. Run: stripe listen --forward-to localhost:3001/payments/stripe
#   3. Copy the webhook signing secret (whsec_...) shown in terminal
#   4. Enter both API key and webhook secret in Admin UI PaymentMethod config
#
# Get your test API keys from: https://dashboard.stripe.com/test/apikeys
# - Secret key (sk_test_...) - for Vendure backend
# - Publishable key (pk_test_...) - for storefront (see apps/shofar-store/.env.example)